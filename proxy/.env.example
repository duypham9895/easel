# MiniMax API key — obtain from https://www.minimax.io
MINIMAX_API_KEY=

# MiniMax model name (default: MiniMax-M2.5)
MINIMAX_MODEL=MiniMax-M2.5

# Static token the Expo app sends in X-Client-Token header
# Generate a secure random value: openssl rand -hex 32
CLIENT_TOKEN=