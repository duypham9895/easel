# BUG_20260308_002 — Fix Notes

## Fix Summary

Removed the conditional `if` block from the `deploy-functions` job in `.github/workflows/supabase.yml` so that Supabase Edge Functions deploy on every push to `main`, matching the existing `migrate` job behavior.

## Change Details

**File:** `.github/workflows/supabase.yml`

**Before (lines 54-60):**
```yaml
deploy-functions:
  name: Edge Functions
  runs-on: ubuntu-latest
  if: |
    github.event_name == 'workflow_dispatch' ||
    contains(toJson(github.event.commits.*.modified), 'app/supabase/functions/') ||
    contains(toJson(github.event.commits.*.added), 'app/supabase/functions/')
```

**After:**
```yaml
deploy-functions:
  name: Edge Functions
  runs-on: ubuntu-latest
  # No condition — runs on every push (idempotent, same as migrate job)
```

## Why This Fix Works

1. `supabase functions deploy` is **idempotent** — it checksums function source and only re-deploys if code changed. The same reasoning already applies to the `migrate` job (`supabase db push` is idempotent).
2. Removes the fragile `contains(toJson(github.event.commits.*.modified), ...)` pattern that fails for large multi-file commits due to GitHub payload truncation.
3. Ensures edge functions are always deployed, preventing the silent 404 failures that have been occurring daily.
4. Overhead is minimal (~30s per CI run for 2 functions).

## Commit 2 — Edge Function Auth Fix

After deploying, the function returned 401 instead of 200. The custom auth check in both functions (`authHeader !== Bearer ${serviceKey}`) was redundant with Supabase's built-in JWT gateway verification and failed on first deploy.

**Files:** `app/supabase/functions/notify-cycle/index.ts`, `app/supabase/functions/notify-sos/index.ts`

**Change:** Removed the custom `Authorization` header comparison (lines 56-64 in both functions). Auth is now handled by Supabase's API gateway which verifies JWTs by default.

## Files Changed (Total)

| File | Change |
|------|--------|
| `.github/workflows/supabase.yml` | Removed `if` condition from `deploy-functions` job, updated header comment |
| `app/supabase/functions/notify-cycle/index.ts` | Removed custom auth check (7 lines) |
| `app/supabase/functions/notify-sos/index.ts` | Removed custom auth check (7 lines) |

## Verification Results

- Supabase workflow run 22819805509: `Edge Functions` job **succeeded** (deployed both functions)
- Notify Cycle workflow run 22819815240: **HTTP 200**, `{"success":true,"sent":0}`

## Post-Deploy Action

- **Check Supabase Dashboard** for `notify-sos` Database Webhook — if not configured, create it manually (Table: `sos_signals`, Event: INSERT, Function: `notify-sos`)

## Self-Review Checklist

- [x] Root cause addressed directly (removed fragile conditional deployment gate + broken auth check)
- [x] No unintended side effects (migrate job unchanged, other workflows untouched)
- [x] Existing behavior preserved (workflow_dispatch still works)
- [x] Verified end-to-end: function deployed + invoked + returned HTTP 200
- [x] No hardcoded values, no debug logs
- [x] Code matches existing patterns (mirrors migrate job's unconditional approach)
